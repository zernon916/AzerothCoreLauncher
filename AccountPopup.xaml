<Window x:Class="AzerothCoreLauncher.AccountPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Account Info" Height="600" Width="900" WindowStartupLocation="CenterOwner">
    <Grid Background="#FF1E1E1E" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Account Name -->
        <StackPanel Grid.Row="0" Margin="0,10">
            <TextBlock Text="Account:" Foreground="White" FontWeight="Bold"/>
            <TextBlock x:Name="TxtAccountName" Foreground="LightGreen" FontSize="16" FontWeight="Bold" Margin="0,5"/>
        </StackPanel>
        
        <!-- Account Info -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="IP Address:" Foreground="White"/>
            <TextBlock x:Name="TxtIPAddress" Foreground="LightGreen" Margin="0,5"/>
            <TextBlock Text="Last Login:" Foreground="White" Margin="0,10,0,0"/>
            <TextBlock x:Name="TxtLastLogin" Foreground="LightGreen" Margin="0,5"/>
        </StackPanel>
        
        <!-- Characters -->
        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Characters on Account:" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>
            <DataGrid x:Name="DgCharacters" Height="200" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Race" Binding="{Binding Race}" Width="80"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="80"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="60"/>
                    <DataGridTextColumn Header="Online" Binding="{Binding IsOnline}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="BtnBanAccount" Content="Ban Account" Width="120" Height="30" Margin="5"/>
            <Button x:Name="BtnUnbanAccount" Content="Unban Account" Width="120" Height="30" Margin="5"/>
            <Button x:Name="BtnBanIP" Content="Ban IP Address" Width="120" Height="30" Margin="5"/>
            <Button x:Name="BtnViewBanHistory" Content="View Ban History" Width="140" Height="30" Margin="5"/>
        </StackPanel>
        
        <!-- Close Button -->
        <Button Grid.Row="4" Content="Close" Width="100" Height="30" HorizontalAlignment="Center" Margin="0,10" Click="BtnClose_Click"/>
    </Grid>
</Window>
